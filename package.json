{
  "name": "info-lomba",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Katalog dan arsip lomba mahasiswa dengan dashboard admin",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "admin": "node scripts/admin.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.117.1"
  }
}
